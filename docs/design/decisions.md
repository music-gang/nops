# Decision log

Design decisions in chronological order. A row is added **in the same commit**
that takes or changes a decision. The extended reasoning lives in
[philosophy](../philosophy.md) and the other documents.

| Date | Decision |
|---|---|
| 2026-09-23 | Go, rewritten from scratch. Reused from nomad-gitops: parse via `/v1/jobs/parse`, plan-then-write, CAS register, supersede, diff redaction. |
| 2026-09-23 | State in SQLite (`modernc.org/sqlite`); per-job lock through a partial unique index. |
| 2026-09-23 | Policy and hooks as flat `nops_*` meta keys (custom blocks do not pass `/v1/jobs/parse`). |
| 2026-09-23 | Hook = a `parameterized` job in the repo, synced by nops; idempotent dispatch with token `<deployment_id>:<phase>`. |
| 2026-09-23 | **pre_hook runs AFTER approval** (order changed from the initial spec): with migrations and backups nothing must run before the human OK. For pre-pulling the order does not change downtime. |
| 2026-09-23 | Apply = just a CAS register; the update is carried out by Nomad, no explicit stops. |
| 2026-09-23 | Active engine (not lazy). `pending_approval` deployments are revalidated on every cycle: index changed → `superseded` (+ a new deployment if there is drift); empty plan → `completed` no-op. No time-based expiry. |
| 2026-09-23 | Dashboard auth via the reverse proxy header (`Remote-User`) (**replaced on 2026-09-24 by OIDC, see below**); notifications via a generic webhook. |
| 2026-09-23 | Verified on `nomad agent -dev` 2.0.3: (1) `/v1/jobs/parse` rejects a `nops {}` block ("Unsupported block type"); (2) `Variables` works, with no value → "Unset variable"; (3) dispatch with undeclared meta → 500 "unpermitted metadata keys", with a missing required meta → 500; (4) `-idempotency-token` (API `IdempotencyToken`) returns the same dispatched job without a new evaluation, **even after the dispatched job has completed** (`dead`) → redispatching after a crash does not rerun the hook. |
| 2026-09-23 | Invalid meta (any ERROR) → policy `none` for the whole job, not just for the wrong key. |
| 2026-09-23 | Store: a single SQLite connection (serialized writes); `Transition` is the only way to change `state`, validates the transition table; `From` is required (compare-and-set on the state). |
| 2026-09-23 | Go module: `github.com/music-gang/nops`. |
| 2026-09-23 | CLAUDE.md holds only the working rules; all system documentation lives in `docs/`. |
| 2026-09-23 | Everything is written in English: code, comments, docs, examples, README, CLAUDE.md and commit messages. |
| 2026-09-23 | Commit messages follow the Angular convention (`type(scope): subject`). |
| 2026-09-23 | `nomadx` exposes only `RegisterCAS`: there is no register without the enforce-index check, so invariant 2 holds by construction. |
| 2026-09-23 | Verified on Nomad 2.0.3: every CAS failure is an HTTP 500 whose message starts with `Enforcing job modify index` (three variants: `job already exists`, `job exists with conflicting job modify index: N`, `job does not exist`), so `nomadx` recognises it by that prefix. A missing job is a real 404. The dispatch idempotency token travels as the `idempotency_token` query parameter (`WriteOptions.IdempotencyToken` in the Go client, not `DispatchOptions`). |
| 2026-09-23 | Registering a spec identical to the live one passes the CAS check and leaves the live `JobModifyIndex` unchanged, but the register response can carry a different index (raw HTTP: response 62, live 61). The engine re-reads the live job instead of trusting the response index. |
| 2026-09-23 | Workflow: pull requests into a protected `main` (ruleset: PR required, checks `test`/`lint`/`integration`/`pr-title`, up-to-date branch, linear history, no force push or deletion, no bypass). Squash merge only, so `main` is a straight line with one Angular-style commit per PR; the PR title is the commit subject. Zero required approvals because there is a single maintainer; CI is the gate. |
| 2026-09-23 | License: Apache-2.0, copyright holder Music Gang. Chosen for the explicit patent grant, inbound=outbound contributions and because it is the default of the Go/cloud-native ecosystem (and of nomad-gitops). All current dependencies (MPL-2.0, BSD-3, Apache-2.0) are compatible. |
| 2026-09-23 | Supply chain: GitHub Actions pinned to full SHAs (repository setting `sha_pinning_required`), read-only workflow token, workflows cannot approve PRs, Dependabot for Go modules and Actions. Nomad in CI is pinned to 2.0.3 with a SHA256 check. `govulncheck` is not a required check so a new advisory cannot block unrelated PRs. CodeQL default setup, secret scanning and push protection are on. |
| 2026-09-23 | `hooks.Runner.Run` blocks until the hook reaches a terminal state and is idempotent: every step (row, dispatch, dispatched job ID, outcome) is persisted so calling it again resumes. A hook that fails or times out is a `Result`; only Nomad or SQLite failures are errors (the engine retries). The `Request` carries the target job spec and commit rather than ready-made meta, so every hook misconfiguration (missing job, not a hook, not batch, unprovidable required meta, image collision) becomes one persisted `failed` in a single place. |
| 2026-09-23 | Hook outcome: any `failed`/`lost` allocation → failed; success needs the dispatched job `dead` and every allocation `complete` (a multi-group hook with a group still pending is not a success); a completed allocation desired `stop` → failed (stopped from outside); no allocation yet → keep waiting until the timeout. The failure message uses the task's `Terminated` (or driver/setup failure) event, not the last one: on Nomad 2.0.3 the last event is "Not Restarting: Policy allows no restarts", which hides the exit code. |
| 2026-09-23 | Hook timeout: counted from `started_at`, checked after reading the outcome; on expiry `StopJob` (no purge) runs **before** `timed_out` is saved. The decision only depends on persisted data and stopping is idempotent, so a crash between the two retries the stop; a stopped allocation seen past the deadline is read as our own stop (`timed_out`), not as an outside stop. Verified on Nomad 2.0.3: a finished batch allocation is `complete`/`run`, a stopped one `complete`/`stop`. |
| 2026-09-23 | A hook run is saved as `running` BEFORE `Dispatch` is sent (invariant 7), and the dispatched job ID right after. A resumed `running` run without a dispatched job ID is looked up by its idempotency token (`nomadx.FindDispatched`, using `DispatchIdempotencyToken` on the dispatched job) and adopted, so a crash or failed save after Nomad accepted the dispatch neither runs the hook twice nor leaves the dispatched job running past its timeout. If no dispatched job matches, the run is `failed` ("outcome unknown"), never dispatched again: it may have run and been garbage-collected, and then the token no longer deduplicates. The same applies to a saved dispatched job that has disappeared. Cost: a crash or a `Dispatch` error in the instant between the marker and Nomad receiving the request also fails the hook, loudly, instead of retrying it. The recovery fallback "look for children carrying `nops_deployment_id`" is replaced by the exact token match. |
| 2026-09-23 | Roadmap in one file, `docs/roadmap.md`, so a session that only has the repo (cloud, phone) knows what is done and what is next: no Issues or Projects, which would be a second ledger and are not always reachable. "In flight" is derived from the branch name (`type/<task-id>`), so there is nothing to keep in sync; "done" is set in the PR that finishes the task. Each task carries what to read, its dependencies and the notes that used to live in a private handoff note. A task with open design questions is `plan first`: a draft PR with the plan, no code, until the maintainer answers. |
| 2026-09-23 | Cloud sessions commit unsigned and open the PR; locally the maintainer keeps committing with GPG. Safe because the squash commit on `main` is created and signed by GitHub and the ruleset does not require signed commits; the branch commits are squashed away. A task given to a cloud session is the explicit request to commit, push and open a PR; never merging and never pushing to `main` stay. |
| 2026-09-23 | (**Replaced on 2026-09-24: a template with only headings exists again, see the last row.**) No PR template. The repository squashes with the PR description as the commit body (`PR_BODY`), copied verbatim, so the template's HTML comments, headings and checklist ended up on `main` (`a933a4c`, `8052989`; history cannot be rewritten). The PR description is written as a commit body instead, and the "done" checklist lives in `development.md`. Not chosen: `squash_merge_commit_message = COMMIT_MESSAGES` (body from the branch commits: untested with several commits) and cleaning the message by hand in the merge dialog. |
| 2026-09-23 | `docs/vocabulary.md` fixes the terms used in code, docs, commits and PRs. Bare "deployment" means the nops deployment; Nomad's is always "Nomad deployment". The hook's Nomad-side job is a "dispatched job" in docs (`child` stays acceptable in code for the parent/child link). A new or renamed concept updates the page in the same PR. |
| 2026-09-23 | Hook timeouts are rounded up to whole seconds (the store keeps `timeout_s`): `1500ms` → `2s`. Rounding down would time a hook out early; rejecting would add a failure path for a value `nops_*_hook_timeout` accepts. |
| 2026-09-23 | `nops_image_<task>` sanitizes the task name (non-alphanumeric → `_`); two tasks mapping to one key with different images make the hook `failed`. |
| 2026-09-23 | Configuration: every option has a flag and a `NOPS_<NAME>` variable, precedence flag > variable > default; an empty variable counts as unset. Every invalid value is reported at once, prefixed with its source. |
| 2026-09-23 | nops reads only its own flags and `NOPS_*` variables: the standard `NOMAD_*` variables are ignored and the Nomad client config is built field by field, not from `api.DefaultConfig()`. When nops runs as a Nomad job, Nomad sets `NOMAD_NAMESPACE`, `NOMAD_REGION` (and `NOMAD_TOKEN` with workload identity) in the task: with the defaults nops would silently work on its own namespace. It also keeps the naming uniform and every setting in one documented table. Verified in `nomad/api`: only `DefaultConfig` reads the environment; `NewClient` fills only an empty `Address` from it, and nops always sets one. |
| 2026-09-23 | Secrets are passed as file paths (`-git-token-file`, `-nomad-token-file`), read once at startup and trimmed, so they never appear in `ps` or in the job spec's `args`; as a Nomad job they come from a `template` (restart on change). Not chosen: env-only secrets (an exception to "every option has a flag") and token flags (visible in the command line). |
| 2026-09-23 | Git authentication: HTTPS token only (basic auth, `-git-username`, default `git`), which requires an `https://` URL. No SSH keys: a token covers GitHub, GitLab and Gitea with less to configure. |
| 2026-09-23 | Diff redaction (`internal/redact`) is a denylist on the names Nomad gives the diff fields, verified against a real Nomad 2.0.3 plan (kept as a test fixture and re-checked by an integration test): every `Env[...]` whatever its name, the template body `EmbeddedTmpl`, `GetterHeaders[...]`, every field below a check `Header`, `user:password@` in any URL, and any field or object name containing `password`, `token`, `secret`, `auth`, `credential`, `privatekey` or `apikey` (case-insensitive, `-` and `_` ignored). The last four words extend the list reused from nomad-gitops, so that `Authorization` and `X-Api-Key` headers are covered. Only the body of a template is redacted, not `DestPath` or `ChangeMode`, which carry no secret and help the reviewer. The value becomes `<redacted>` (empty stays empty) and the field name and change type stay. False positives (`Meta[author]`) are accepted over leaks; a secret inside a value under a harmless name (`args`) is a known limit, documented in dashboard.md. |
| 2026-09-23 | Notifications have built-in adapters: a generic JSON webhook, Discord, Slack, ntfy and Gotify. This replaces "Slack/Discord through an external adapter": the formats are a few lines each, and a self-hosted setup should not need n8n just to reach a chat. Each adapter has its own options (`-notify-<adapter>-url[-file]`, `-notify-<adapter>-token-file`) instead of a shared `url` + `kind` + `token`, so each is validated on its own without cross rules, and something specific to one adapter stays in its own options. An adapter is on when its URL is set, and several can be on at once. `-notify-url` is removed. |
| 2026-09-23 | Secrets in notifications: Discord and Slack webhook URLs carry their token, so they are files like every other secret; ntfy and Gotify take a plain server URL and a token file (required for Gotify). Neither the WARN of a failed delivery nor a config error ever contains a URL read from a file (`*url.Error` is unwrapped). |
| 2026-09-23 | A notification is sent synchronously, once per adapter, bounded by `-notify-timeout`, with no retry. A failure is a WARN and never reaches the engine; the other adapters still receive it. The engine calls it after the transition is saved and may run it in a goroutine. |
| 2026-09-23 | `-public-url` is the external URL of the dashboard (nops is behind a proxy and cannot know it). Notifications link to `<public-url>/deployments/<id>`; unset, they carry no link. (**Now required**: the OIDC redirect URL is built on it, 2026-09-24.) |
| 2026-09-23 | Every secret also gets a second, env-only variable with the literal value (`NOPS_GIT_TOKEN` next to `NOPS_GIT_TOKEN_FILE`, and so on for all 8 secret options), with no flag. The earlier "secrets are files" rule was justified by `ps`/`/proc/<pid>/cmdline` exposure, which is real for a flag but not for an environment variable: a variable is only readable from `/proc/<pid>/environ`, by the same user or root, same exposure as a `0600` file. Forcing a file when the operator's own tool already injects a secret into the environment safely (systemd `EnvironmentFile`, `docker --env-file`, a Kubernetes `secretKeyRef`, a Nomad `template` with `env = true`) added friction with no security benefit. Precedence: flag file > variable file > this variable > off. Whether a literal secret ends up in a checked-in job's `env {}` block instead of Vault or a Nomad Variable stays the operator's call; nops cannot enforce that either way. |
| 2026-09-23 | The squash setting changes from "PR title and description" to "PR title and commit details" (`squash_merge_commit_message = COMMIT_MESSAGES`): the commit that lands on `main` is the branch's own single commit, in plain Angular style (no markdown headings — that is what `git log` shows), not the PR description. This is the common practice in projects that squash-merge and care about `git log` (Kubernetes and others): a clean commit for history, a separate, richer PR description for reviewers. The PR description is structured as `## What changes` / `## Why`, each a short bullet list, filled in by hand every time, with an optional `## Notes` for a maintainer decision or a note for the next task; this supersedes the earlier "no comments, headings or checklists" (that rule targeted a template's leftover HTML comments and unfilled checklist items landing on `main`, `a933a4c`/`8052989`, not deliberate, filled-in structure). It also corrects an earlier, mistaken reading of `squash_merge_commit_message = PR_BODY` as meaning the PR description was already copied verbatim onto `main`: in practice every squash so far (#8, #9, #10) was merged with the box cleared to the title alone, so nothing on `main` needs fixing — only the already-merged PR pages did, and were edited to drop the stray attribution lines. |
| 2026-09-23 | A commit or PR made by an assistant carries `Co-Authored-By` and nothing else attribution-wise: no session link or URL, no "Generated with ..." line. `Co-Authored-By` alone is enough to tell who or what wrote it; the rest holds no information for anyone reading `main` later. `docs/development.md#commit-messages` states this so it holds across sessions, regardless of a session's own default attribution instructions. |
| 2026-09-23 | gitwatch reads every `*.nomad.hcl` and `*.nomad` (the older `nomad job init` name) under an optional `-git-path` subdirectory, recursively. The vars file is `<name>.vars.hcl` next to the job file, `<name>` being the file name without `.nomad.hcl` / `.nomad`. Every other file is ignored silently, so a repo can also hold Terraform or Packer files. See [gitwatch](gitwatch.md). |
| 2026-09-23 | Whether a file is a managed job, a hook or neither is decided by its content after the Nomad parse (`nops_role = "hook"`, `nops_managed = "true"`), never by file name or directory: Nomad is the only HCL interpreter, and a naming rule could disagree with the content. A hook is looked up by its parsed job ID in the same snapshot; two files with the same job ID are both ignored, with an ERROR. |
| 2026-09-23 | A failed git fetch is an ERROR that keeps the last good snapshot and retries at the next tick, so detection (and Nomad-side drift) keeps running; `-git-path` missing in a new commit counts as a failed fetch, never as "every job removed". Only the initial clone is fatal. Each new head is a fresh shallow in-memory clone (after a cheap `ls-remote` shows a change), so memory stays constant and a force-push is just a new head. |
| 2026-09-23 | `internal/gitwatch` uses `github.com/go-git/go-git/v5` with `git.CloneContext(ctx, memory.NewStorage(), nil, ...)`: a nil worktree gives a bare, in-memory clone, so nothing ever touches disk and there is no checkout step to skip. `SingleBranch` + `Depth: 1` + `Tags: NoTags` keep each clone to the one commit needed. Tests run against a local bare repository over `file://`, which is how go-git's own test suite exercises the file transport; it shells out to the `git` binary (`git-upload-pack`), so the tests skip when `git` is not on `PATH`, an assumption already implied by the rest of the workflow (checking out this repository needs it too). |
| 2026-09-24 | `deployments.job_spec` keeps the full, unredacted parsed job: the database is local (same trust boundary as the HCL files and Nomad's own copy of the job), apply needs the exact spec that was planned and approved, and re-reading it from git at apply time does not work in general since gitwatch only keeps the current head. Mitigation: the database file, and its `-wal`/`-shm` sidecars, are created and kept at permission `0600` (`store.Open`); the dashboard never renders `job_spec`. See [engine-detection](engine-detection.md#job_spec-keeps-the-full-unredacted-spec). |
| 2026-09-24 | The drift of a job under policy `none` is kept only in memory (`Engine.Observations()`), rebuilt from scratch on every detection cycle, not in a new table: it is always a function of the current git head and the current live job, so there is nothing that must survive a restart. See [engine-detection](engine-detection.md#drift-of-a-none-policy-job-lives-only-in-memory). |
| 2026-09-24 | A hook declared but missing from the repository (`nops_pre_hook`/`nops_post_hook` names no hook file in the current snapshot) fails the deployment at detection (`detected → failed`, with a notification), rather than waiting for an approval that is guaranteed to fail at the hook step. This is in addition to, not instead of, the runtime check `hooks.Runner` does again at dispatch time, since git can change in between. |
| 2026-09-24 | A new deployment is not created for a job whose latest one is `failed` or `rejected` with the same `spec_hash` and the same live index (`cas_index`): nothing that produced that outcome changed. A new commit or a change to the live job (a different index) creates a new deployment again, consistent with invariant 2 ("a CAS conflict leads to `failed` and a re-detection"). |
| 2026-09-24 | Revalidating an existing `detected`/`pending_approval` deployment (supersede on a spec change, on the live index changing, or completing an empty plan) compares `spec_hash`, never `commit_sha`: a commit that does not touch a job's file must not disturb its deployment. `spec_hash` itself is computed before task groups with a `scaling` block take their `Count` from the live job (nops does not fight the autoscaler), so the autoscaler moving a count never supersedes a pending approval. |
| 2026-09-24 | Hook sync (register or update, plan + CAS) runs for every hook job found in the snapshot regardless of any one deployment's phase: a hook updated by a newer commit is synced immediately, even while an older deployment is still `pre_hook`/`post_hook` for the previous version. Hooks are already required to be idempotent; this adds tolerating a newer version of themselves being dispatched mid-deployment. |
| 2026-09-24 | `internal/engine`'s parse cache and in-memory observations are rebuilt from scratch and swapped in whole on every detection cycle, never mutated incrementally: a job or file no longer in the snapshot is dropped from both automatically, so neither grows over the life of the process. |
| 2026-09-24 | `pending_approval` is moved forward by `Engine.Approve`/`Reject`, not by `web` calling `Store.Transition` directly: `Approve` refuses if the caller's `spec_hash` no longer matches the deployment's current one (invariant 3), then picks `pre_hook` or `applying` by the same rule detection uses for `auto`. `web` only ever calls these two methods for a decision. |
| 2026-09-24 | `Store.SetApplied` persists `applied_index`/`eval_id` while a deployment stays `applying` (no schema change, guarded on `state = 'applying'`), because invariant 7 requires them saved before nops waits for health and `Store.Transition` only writes on a state change. |
| 2026-09-24 | Apply health: a Nomad deployment for the applied job version reaching `successful`, or, when there is none (a `batch` job, or an `update` stanza that produces none), the allocations of that job version reaching `running` (`running`/`complete` for a batch task group). `nomadx` gains a method to read a Nomad deployment by ID. |
| 2026-09-24 | The apply timeout counts from the `→ applying` event's timestamp (`Store.AppliedSince`, no schema change), not from a new column: the event is already written by `Transition` and already gives whole-second precision, same pattern as a hook's `started_at`. |
| 2026-09-24 | An apply that does not become healthy in time only gets `Transition` to `failed`: no `nomad deployment fail`, no automatic revert. nops automates a manual GitOps process; what Nomad already does better (rolling update, canary, the job's own `auto_revert`) stays entirely Nomad's — nops never steps in. |
| 2026-09-24 | A deployment that reached the register (`applied_index != 0`) and then `failed` blocks new deployments for the same job while the latest one keeps that `spec_hash`, whatever the live index is now (only a new commit, a different `spec_hash`, unblocks it). Without this, a job with `auto_revert` on loops forever: apply fails to become healthy, Nomad reverts it (changing the live index), the existing "same `spec_hash` and same `cas_index`" retry rule no longer matches the new index, and nops re-applies the same rejected spec every cycle. This extends, not replaces, the existing retry rule for a failure that never reached the register. |
| 2026-09-24 | `Observation` gains `BlockedBy`/`BlockedReason`, filled by detection whenever a retry rule suppresses a new deployment for a job's drift, so a job stuck by either retry rule is visible in the dashboard instead of silently never converging. An explicit "retry without a new commit" dashboard action is deliberately left out for now: nothing in this design blocks adding one later, and it would be a thin wrapper over the same "create a deployment" path. |
| 2026-09-24 | Apply health (`engine-apply`) is decided from `nomadx.Client.LatestDeployment` (wraps `Jobs().LatestDeployment`, `nil` when the job has none) matched against `applied_index`, not from the register's `EvalID` and `Evaluations().Info` as first proposed: it needs no eval at all, which the crash-recovery register path does not always have, and adds no new `nomadx.Evaluation` wrapper. No match falls back to the allocations of the applied job version (`nomadx.Alloc` gains `JobVersion`), reusing the same failed/running/complete logic already used for hook outcomes. |
| 2026-09-24 | Workflow simplified after running `engine-apply` through it end to end. `plan first` no longer means a draft PR: the plan and open questions go to the maintainer in the session, before any branch exists, since the draft PR added a step (open it, push docs, wait) with no benefit over discussing it there directly, and its content (the design, the decisions) still ends up as a design page and decision-log rows once coding starts, same as before. A branch is no longer capped at one, amended commit either: with a single maintainer who squashes and edits the merge message by hand, keeping the branch to one commit added the overhead of `git commit --amend --force-with-lease` on every change for no reader who would otherwise see a messier history — a branch now carries as many commits as the work needs, each an honest step, and the maintainer turns them into `main`'s one commit at merge time. |
| 2026-09-24 | Recovery after a restart (`engine-recovery`) is the first cycle of `RunApply`, not a separate startup pass: `RunApply` already runs a cycle before its ticker, and every apply step resumes from the store alone, so no function is exported and no lock is added. It stays asynchronous (one goroutine per deployment) because a hook step blocks until its hook ends and a blocking pass would delay the dashboard and detection. Not chosen: a synchronous `Recover` before starting the loops (the same work, plus a startup that can hang for a hook's whole timeout). See [engine-apply](engine-apply.md), decision 8. |
| 2026-09-24 | Apply health fails a deployment when the live job's index is no longer `applied_index` and no Nomad deployment tracks that index ("job modified outside nops while waiting for health"), instead of falling back to the allocations of the live version, which by then belong to someone else's spec. Found while writing the crash-window tests: a restart is the likeliest moment for an outside edit to slip in between the register and the health check. Not chosen: leave it as a known limitation, or a separate roadmap task for a few lines. |
| 2026-09-24 | The dashboard logs people in with built-in OpenID Connect (`coreos/go-oidc/v3`, `golang.org/x/oauth2`, `gorilla/securecookie`); `-auth-header` / `Remote-User` is gone. A header cannot be verified: anything that reaches nops's port without going through the proxy (another allocation, the LAN, the tailnet) could approve a deployment by sending it, and a network mistake would fail silently. A signed ID token is checked by nops itself, so a proxy in front is only for TLS. Not chosen: the trusted-proxies allowlist for the header (works, but the safety is still one firewall rule away from gone), Tailscale-only identity (ties nops to one network). Costs: three dependencies, `-public-url` and the OIDC options become required, and nops needs a client at the provider. See [dashboard](../dashboard.md#authentication). |
| 2026-09-24 | **Every** dashboard page needs a login, reads included, not only the writes: the plan diff says what runs on the cluster. Only `/auth/*`, the git webhook (its own secret) and the health check are open. |
| 2026-09-24 | The allowlist is required: `-oidc-allowed-users` (username or email) and/or `-oidc-allowed-groups` (the `groups` claim), and with both empty nops does not start. Anyone who can authenticate at the provider would otherwise be able to approve a deployment, and a provider often serves more people than the cluster's operator. An email counts unless the provider says `email_verified: false`. The allowlist is checked at login only: a removal takes effect when the 12 hour session ends or nops restarts. |
| 2026-09-24 | The session key is random at startup, not a configured secret: nothing to manage or leak, and the cost of a restart is one login, one redirect when the provider still has its session. Not chosen: `-session-key-file` (sessions surviving restarts, one more secret). |
| 2026-09-24 | OIDC discovery is lazy: nops never contacts the provider at startup and retries it at every login (503 and an ERROR while it fails). A provider outage must not stop the engine, since `auto` deployments do not need a human. Not chosen: failing at startup (simpler, but an Authentik restart would also stop the deployments). |
| 2026-09-24 | The ID token's claims are completed from the userinfo endpoint when the allowlist needs a claim it lacks (`groups`, or any identity), and a userinfo answer for another subject is an error: Authelia keeps most claims out of the ID token by default. `groups` is requested as a scope only when `-oidc-allowed-groups` is set. |
| 2026-09-24 | CSRF protection is Go's `http.CrossOriginProtection` (`Sec-Fetch-Site`, or `Origin` against `Host`) plus a `SameSite=Lax` session cookie, not a token in every form: no token to thread through the templates, and a browser sends the headers on every request. Requests with neither header (non-browser clients) are allowed and still need the session cookie. This replaces "POST only with a CSRF token". |
| 2026-09-24 | The git webhook is authenticated with a secret per forge (`-webhook-secret-file`, task `web`): the HMAC-SHA256 of the body for GitHub and Gitea, a constant-time comparison of `X-Gitlab-Token` for GitLab. Not chosen: a token in the URL (it lands in the proxy's access log), or no authentication (a fetch is cheap and triggers coalesce, but an open endpoint is one more thing to explain). |
| 2026-09-24 | `web`'s pages stay on `html/template` (already the standing choice, not templ): no codegen, no CLI to pin, no generated files to keep in sync, for about five pages. The safety templ would give (a broken template caught before it reaches a browser) comes instead from parsing every template once at startup (`New` fails loud on a parse error) and a render test per page. htmx (vendored under `/static`, not a CDN, so the CSP stays `script-src 'self'`) is progressive enhancement only: every action is a plain `<form method=post>` that works with no JS, and htmx only adds `hx-boost` and polling of a deployment's status fragment while it is non-terminal. Not chosen: templ (weighed against overengineering for this scope), Alpine.js on top of htmx (nothing in the pages needs client-only state `<details>` does not already give). |
| 2026-09-24 | (**Replaced on 2026-09-24 by Primer, see the last rows.**) The dashboard's look is a reading of Airbnb's public design system (`github.com/VoltAgent/awesome-design-md`, `airbnb`: white/ink canvas, soft rounded corners, modest type weights, one accent used sparingly, one loud typographic moment) rather than Tailwind or Pico: hand-written CSS with custom-property tokens, no build step, redefined for dark mode under `prefers-color-scheme` (Airbnb's public site has none). The accent is nops's own deep teal, not Airbnb's Rausch pink-red, which would read as "danger" right next to the failed/rejected states in the same palette. Fonts are Inter and JetBrains Mono (Inter is the open-source substitute the Airbnb design notes name for Cereal), vendored as two variable-font `.woff2` files under `/static/fonts` rather than loaded from Google Fonts, so the CSP stays `font-src 'self'` and the page never depends on that request succeeding. |
| 2026-09-24 | The plan diff renders Nomad's `JobDiff` by walking it recursively in `html/template` itself (nested `{{template}}` calls over `Fields`/`Objects`/`TaskGroups`/`Tasks`), not by pre-rendering to a string in Go: every value stays contextually auto-escaped by the template engine, and the same `diff` template serves both the deployment page and the drift page. A `<details>` is open only when its `Type` is not `"None"`, so only what changed is expanded. A value equal to `redact.Marker` renders as a pill (`diffCell`, the one function that returns `template.HTML`, escaped by hand with `template.HTMLEscapeString` first) instead of literal text, so a redacted secret reads as "a secret changed here" rather than the string `<redacted>` sitting in a diff. |
| 2026-09-24 | The deployment page polls its own status instead of the browser reloading: `GET /deployments/{id}/status` returns an htmx fragment (the decision rail, not the diff, which cannot change for one deployment) that the page fetches every 3s via `hx-trigger`/`hx-swap=outerHTML` while the deployment is non-terminal. The fragment stops polling itself by simply not re-emitting `hx-trigger` once `isTerminal` is true, rather than a client-side condition: `isTerminal` is one function, registered as a template func, so the "is this deployment still moving" logic is not duplicated between Go and JavaScript. |
| 2026-09-24 | The dashboard gains a second login backend, local users (`-auth-mode=basic`: an operator-maintained `username:bcrypt-hash` file, no SQL table), alongside OIDC (`-auth-mode=oidc`), mutually exclusive via a required `-auth-mode` with no default. Reason: `internal/config` made OIDC unconditionally required to even start nops, a real barrier for the self-hosted/personal cluster CLAUDE.md targets when no OIDC provider is already running; a file matches how nops already treats git, an external source the operator edits, nops never writes to it. A local user is philosophically the same trust model as OIDC, not the rejected `Remote-User` header: nops itself verifies the credential (bcrypt compare), it does not trust something set upstream of it. Decided and built before `wiring` specifically so `cmd/nops`'s startup is written once against the final shape. Not chosen: both backends active at once (doubles the login surface to reason about for a personal tool), a `nops` subcommand to hash a password (kept out of scope; `htpasswd -nB` or any bcrypt one-liner already does it), YAML/JSON for the users file (htpasswd-style needs no parser beyond a `:` split and is generatable with a tool most self-hosters already have). |
| 2026-09-24 | An unknown username in local-users login still runs a bcrypt compare, against a fixed hash computed once at startup (`timingSafetyPassword`), so a wrong username and a wrong password are indistinguishable by response time or content (the same generic "Invalid username or password"). Free correctness (bcrypt is already the slow part; comparing against something instead of short-circuiting costs nothing extra in code), so there was no reason not to. |
| 2026-09-24 | The session, cookie and `Require`/logout machinery, previously private to the OIDC `Auth` type, is extracted into an unexported `session` type (`internal/web/session.go`) shared by both login backends, rather than duplicated: it is security-sensitive (cookie flags, signing, CrossOriginProtection) and a second, slightly-different copy is exactly the kind of drift that produces a bug one backend has and the other does not. `web.Options.Auth` changes from the concrete `*Auth` to a small `Authenticator` interface (`Register`, `Require`) that both `*Auth` and the new `*BasicAuth` satisfy; every existing `auth_test.go` case stayed green, unchanged, through the extraction. |
| 2026-09-24 | Found while wiring `Authenticator` into `server.go`: `routes()` never called `Auth.Register(mux)`, so `/auth/login`, `/auth/callback` and `/auth/logout` were never mounted on the dashboard's own mux in the merged `web` task — every `internal/web` test minted a session cookie directly (`mintSession`), bypassing the real login routes entirely, so nothing caught it. Fixed as part of this change (`s.auth.Register(mux)` in `routes()`); real login-flow tests were added for both backends (`auth_basic_test.go`, and the OIDC path is already covered end to end by the pre-existing `auth_test.go`). |
| 2026-09-24 | `cmd/nops` logs JSON to stderr (`slog.NewJSONHandler`), not text: the project already commits to structured `log/slog` everywhere (docs/error-handling.md), and JSON is the more useful default for a self-hosted Nomad cluster's usual next step (`journalctl -o cat \| jq`, Loki, Promtail) without losing readability for a one-off `journalctl` glance. `main()` is a thin wrapper around a factored-out `run(ctx, cfg, log) error`, so the whole wiring is one reviewable, unit-testable function that returns errors instead of calling `os.Exit` itself; `main` only maps that error (and `config.Load`'s `flag.ErrHelp`) to an exit code. |
| 2026-09-24 | The dashboard's `http.Server` gets a 10s `Shutdown` grace period on `SIGINT`/`SIGTERM`; the three background loops (`gitwatch.Watcher.Run`, `Engine.RunDetection`, `Engine.RunApply`) get none of their own, since they already `select` on `ctx.Done()` and a hook wait respects the same `ctx` down through `applyStep` — cancelling one context unwinds all three well under a second (confirmed by the binary smoke test), so a shutdown timeout there would only ever paper over a real bug, not a legitimately slow path. |
| 2026-09-24 | Found while wiring `run()`: `config.Load`'s doc comment ("usage and flag errors are written to `out`") only covers `flag.ErrHelp` and a bad flag's own parse error — a `check()` validation error (a missing required option, and so on) is only ever *returned*, never printed by `Load` itself. `main()` printing nothing and exiting 1 on such an error would have been silent and undiagnosable at the one place an operator actually hits it (a typo'd flag on first run). Fixed in `cmd/nops/main.go`: `main` prints a returned non-`ErrHelp` error to `os.Stderr` itself before exiting. |
| 2026-09-24 | `run()` derives its own cancellable child of the signal context (`context.WithCancel`) rather than using the signal context directly, so that an `http.Server.ListenAndServe` failure (a bad `-listen-addr`, the port already in use) also stops the three background loops and `run` returns an error, instead of leaving them running forever waiting on a `ctx.Done()` that a bind failure alone would never trigger — caught while writing the binary smoke test's failure paths, not by the smoke test itself (which only exercises the success path). |
| 2026-09-24 | The wiring task's "Done when" smoke test (`tests/integration/nops_test.go`) runs with `-auth-mode=basic`, not `oidc`: it needs no OIDC provider in CI, a fixed local users file is one line of setup, and the choice between the two backends is already covered by a plain unit test (`cmd/nops/main_test.go`'s `newAuthenticator`), so the smoke test's job is only to prove the binary as a whole starts, serves and shuts down, not to re-verify either login backend. |
| 2026-09-24 | `-public-url` is no longer required: left unset, it defaults to `http://<host>:<port>` derived from `-listen-addr` (`localhost` in place of an empty or wildcard host), for `-auth-mode=basic` with no reverse proxy in front, where there was nothing meaningful to type there. The default applies to `-auth-mode=oidc` too, deliberately not special-cased: it is very likely wrong for a real provider's registered redirect URI, same as forgetting it used to be a hard stop, just no longer one at startup — `docs/dashboard.md`'s OIDC setup section says to set it explicitly. Computed once, in `config.check()` (already the place cross-option rules live), not in the option's own `set`, so it needs no assumption about table order between `-listen-addr` and `-public-url`. |
| 2026-09-24 | `examples/` becomes one runnable directory per scenario (target job + its hook + a `<name>.vars.hcl`) instead of loose hook-only files with the deployed job only shown in a comment: the same jobs are what the `acceptance` checklists deploy against a real cluster, and a job that only exists as a comment cannot be pointed to by a running nops. Acceptance runs point nops at this repository itself, on a dedicated `acceptance-run` branch with `-git-path=examples`, so a run needs no fixture repository of its own; triggering a deployment is committing to that branch's `.vars.hcl`. `examples/migrate/` keeps the DB-migration hook contract example even though its target job's placeholder image has no real migration binary, since it is a syntax/contract reference, not one of the three checklists. |
| 2026-09-24 | The manual `acceptance` checklists are dropped in favour of simulated end-to-end integration tests (`tests/integration/e2e_*_test.go`): the real binary, a scratch git repository, the dashboard over HTTP and `raw_exec` jobs and hooks against `nomad agent -dev`. Almost everything the checklists checked is state-machine correctness that a simulation reproduces, and a manual run goes stale silently while a CI test fails when it should. What a simulation cannot say (real pull times, a real restore, the cluster's own ACLs/TLS) is judged by using nops on the cluster, rolling out `none` → `approval` → `auto`. `examples/` stays as the reference for the hook contract, guarded by `TestExamplesParse`, and the decision of 2026-09-24 that tied it to the checklists no longer applies to them. See [development](../development.md#integration). |
| 2026-09-24 | `examples/` need nothing but Nomad: the deployed jobs register their services with `provider = "nomad"` and the hooks find them with `nomadService` in a `template` (a URL for the smoke test, `PGHOST`/`PGPORT` for the backup), instead of `*.service.consul` DNS names. A `service` block with no provider defaults to Consul, and such a job is not even placed on a cluster without it (`${attr.consul.version} semver >= 1.8.0`, checked with `nomad job plan` on an agent with no Consul); the examples had worked only because the maintainer's cluster has Consul. `TestExamplesParse` now fails on a service that is not `provider = "nomad"` and on any mention of Consul. See [hooks](../hooks.md#reaching-the-service-from-a-hook). |
| 2026-09-24 | The page a login goes back to (`?next=`) is kept only if it is a path on the dashboard: `safeNext` refuses every control character and backslash anywhere in it, a `//` or `/\` start, and anything `url.Parse` reads with a scheme or host. It used to refuse only `//`, a backslash and CR/LF, so `/<TAB>/evil.example.com` went out as a `Location` that browsers read as `//evil.example.com` (an open redirect right after a legitimate login, CodeQL alert 1, #25). See [dashboard](../dashboard.md#authentication). |
| 2026-09-24 | The dashboard is redesigned for what an operator does with it, not restyled. Five tasks: is anything waiting on me, decide, is the cluster in line with git, what happened to X, is nops working. Five pages follow from them: Overview `/` (what needs attention: pending approvals, recent failures, blocked jobs, invalid meta; in-progress deployments; the health of git and of the last cycle), Jobs `/jobs` (every managed job, `none` included, with its sync state and last deployment; replaces `/drift`), Job `/jobs/{namespace}/{job}` (its drift, hooks and deployments), Deployment `/deployments/{id}` (a review page: what changes, what will run, the decision on top), Activity `/history`. The pages so far mirrored the tables (pending, history, drift) and had no job as an entity and no signal that nops itself works. The look moves to GitHub Primer (dense one-line rows, relative times with the absolute one on hover, short hashes with the full value on hover, system fonts) in place of the Airbnb reading of the row above, which is replaced when `dashboard-ux` lands. Built in three PRs: `dashboard-data` (the backend the pages need), `dashboard-ux` (pages and look), `dashboard-polish` (after using it). See [roadmap](../roadmap.md#dashboard-ux). |
| 2026-09-24 | `deployments` gains `commit_subject` and `commit_author`, copied from the commit when the deployment is created (migration `0002`). Not looked up later: the clone is shallow and only holds the head, so an older deployment could not say what its commit was. The author is the name only, no email; the subject is the first line. Empty for a deployment created before the columns existed: pages show the SHA alone. The link to a commit is `gitwatch.CommitURL(repoURL, sha)` = `<repo>/commit/<sha>` for an `http(s)` URL with credentials and `.git` dropped, and none for `file://` or scp-style URLs (GitLab redirects that path to its own). |
| 2026-09-24 | Health is kept in memory and never persisted, like `Observations()`: `gitwatch.Watcher.Status()` (`CheckedAt`: the last poll that reached the remote, whether or not there was a new commit; `Error`/`ErrorAt`: the last failure, cleared by the next success) and `Engine.Status()` (end, duration, aborting error, and three counts: managed jobs found, jobs skipped for a Nomad failure, files that did not parse). The counts are there because a cycle that logs an ERROR per job and yields no observation is otherwise indistinguishable, from the dashboard, from a cluster with nothing to do. |
| 2026-09-24 | **Retry** (`Engine.Retry`, `POST /jobs/{ns}/{job}/retry`) lifts a blocked drift without a new commit by marking the blocking deployment retried, not by creating or approving a deployment: the next cycle creates it through the normal path and the policy decides, so under `approval` the retry still waits for a human (invariant 3). Confirmed with the maintainer after weighing "retry = approve", which would approve a spec the person never reviewed on that click. One retry is one attempt. Schema: `retried_by`, `retried_at`. Details and what was not chosen: [engine-apply](engine-apply.md), decision 9. Replaces the "out of scope" note of decision 7 and of the `Observation.BlockedBy` row above. |
| 2026-09-24 | `Store.MarkRetried` writes an event whose `from_state` and `to_state` are the same terminal state, message `retry requested`, instead of a new column or table for the audit: the timeline already reads `events`, and nothing else moves. It is the only write on a terminal deployment, guarded on `state IN (failed, rejected) AND retried_at IS NULL`. The migration also adds `deployments_by_job (namespace, job_id, created_at DESC)` for `ListByJob` and `LatestPerJob` (a few hundred rows in practice, the index is cheap insurance for a page that lists a job's deployments). |
| 2026-09-24 | "Fetch now" (`POST /fetch`) calls the same `Watcher.Trigger` as the git webhook: a non-blocking poll request that coalesces with others, so there is no second way into gitwatch and nothing to wait for. It answers with a redirect, not with the poll's outcome: the outcome is the git head and the poll status the pages show. The route exists only when `web.Options.Trigger` is set. |
| 2026-09-24 | The redirect after the new writes (retry, fetch now) is the constant `/`: no form value reaches the `Location` header. A first version took a `next` value, checked by `safeNext`; CodeQL (`go/unvalidated-url-redirection`) flagged both handlers, since it cannot see through a hand-written validator. It was not exploitable (both routes need a session and refuse cross-site requests, and `safeNext` is strict), but the rule's advice is also the better design: keep user input out of the redirect, and if a write needs to go back to several pages, choose from a server-side list by name (`back=jobs` mapped to a constant path), never take a path or URL. `dashboard-ux` adds that list when a second destination exists. `safeNext` stays for the post-login redirect, where the destination has to come from the request that was interrupted. |
| 2026-09-24 | `dashboard-ux`: the look moves from the Airbnb reading to GitHub's Primer, because the dashboard is for scanning lists, not for browsing: Airbnb's large cards, generous white space and one loud number gave one deployment per screenful and wrapped everything. Primer's tokens (light and dark, following the system), 14px, 6px corners, and **one thing per line**: cut with an ellipsis rather than wrapped, the whole value on hover, relative times with the absolute one on hover, short hashes (12 for a spec hash, 8 for an evaluation, 7 for a commit). System font stacks replace Inter and JetBrains Mono: nothing is downloaded, and the two vendored `.woff2` files and their licenses go. Hand-written CSS still, no Primer bundle (large, and it needs a build). The CSP does not change (`font-src 'self'` simply has nothing to allow). On a narrow screen a row becomes two lines on purpose, secondary columns hide, and a wide diff scrolls inside its own box. |
| 2026-09-24 | **Sync state** of a job (Jobs page and filter), the first that applies: invalid meta (an *error* issue; a warning is not), blocked (`BlockedBy`), awaiting approval, deploying (`detected`, `pre_hook`, `applying`, `post_hook`), drift, in sync. Invalid and blocked come first because they are what a person has to act on and would otherwise hide under "drift". A job the last cycle could not observe has no row (it is not in `Observations()`); its deployments are still on Activity and its Job page. |
| 2026-09-24 | **Needs attention** on the Overview, most urgent first: approvals (oldest first), blocked jobs (with *Retry*), failures nobody dealt with, jobs with a meta error. A failure is listed only if it is the latest deployment of its job, was not retried, is under 7 days old and is not already the reason a job is blocked (one line per problem, not two). A job under policy `none` that drifts is **not** listed (confirmed with the maintainer): leaving it alone is what that policy says, so it is a state on Jobs, not news. No nav counters: every page would pay three queries for a badge. |
| 2026-09-24 | The deployment page shows what Approve will do (pre-hook, register with its CAS index, wait for health, post-hook) read from the meta of the spec nops stored (`meta.Parse` on the stored job): only keys and job IDs, never the spec, so the redaction rule of `job_spec` holds. Only while `pending_approval`; a spec that does not parse is a 500 and an ERROR, like a diff that does not (nops wrote it, so it is a bug or a corrupt database, not something to paper over). Approve still sends the **whole** `spec_hash` in a hidden field whatever the page shows short (invariant 3). The plan diff is folded unless there is something to decide, so what a finished deployment did (hooks, timeline) is not pushed off the page by a diff of a whole new job; a failed deployment that blocks its job says so and offers the retry where its failure is read. |
| 2026-09-24 | The status fragment of the deployment page refreshes three regions with one poll: the head and decision replace themselves (`hx-swap outerHTML`, the polling element), the hooks/timeline and the details column swap **out of band** (`hx-swap-oob`, only in the fragment, never in the page). The diff, the heavy part and the one that never changes while it is polled, is not resent and keeps its open/closed state. |
| 2026-09-24 | `Observation` gains `PreHook` and `PostHook` (the hooks the job's meta declares), so the Job page can show them for a job under any policy, `none` included, which has no deployment to read them from. The reason of a blocked drift no longer names the deployment ID (the page links it from `BlockedBy`; it made the line long and was noise) and says what unblocks it: "failed on the same live job; push a new commit or retry it" (or "was rejected on ..."). |
| 2026-09-24 | A retry goes back to a page chosen **by name** from a fixed list (`back` = `overview`, `jobs`, `activity` or `job`; anything else is the Overview), as the row on `Retry` above promised. `job` is the job's own page built from the engine's copy of its name, not from the request's path, so a request can never choose where the redirect lands. |
| 2026-09-24 | `/drift` stays as a `301` to `/jobs`: bookmarks and old links keep working, and the page's content is a subset of Jobs. `web.Options` gains `Git` (required: the Overview shows the commit and the poll status, so a dashboard without it is not a working one), `CommitURL` (optional: no link without it) and `Now` (optional: the clock relative times count from, so tests do not depend on when they run). The repository link is built from `-git-url` in `cmd/nops` by `gitwatch.CommitURL`; a `file://` or scp-style URL simply has none. |
| 2026-09-24 | The PR template comes back, as `.github/pull_request_template.md` with only `## What changes`, `## Why` and `## Notes`: no HTML comment, no checklist. The reason for having none (the 2026-09-23 row) was that the squash setting copied the PR description onto `main`; since `squash_merge_commit_message = COMMIT_MESSAGES` the description never reaches `main`, so a template can no longer pollute the history, and it gives the structure the description already has by rule. It is headings only so that leaving one unfilled is harmless, and to keep "no boilerplate" true. It applies to PRs opened from the web; `gh pr create --body-file` replaces it. `CONTRIBUTING.md` still described the old flow (description as the commit body, no template) and is corrected. The merge flow is written down as the maintainer does it: one commit is a plain merge, several are cleaned up and the first is kept or the message is rewritten; the PR description never enters the commit. |
| 2026-09-25 | Static files are linked with a version taken from their bytes (`/static/app.css?v=<first 10 hex digits of the SHA-256>`, template function `asset`), instead of a fixed path under a day-long `Cache-Control`. The redesign changed the whole stylesheet at the same address, so a browser that had the old one showed the new pages with the old CSS and looked broken until a forced reload; every future CSS change would do it again. A version from the content changes exactly when the file does and never otherwise, so the cache stays useful, with no build step and nothing to bump by hand. Not chosen: a shorter `max-age` or `no-cache` (every page would revalidate or re-download the CSS), a release version in the query (a CSS change between releases, or a rebuilt binary, would be missed or need a manual bump), and a hash in the file name (needs a build step or a rewrite of the served path). The version is computed lazily and once per file, and a missing file is an error, not a link. |
| 2026-09-25 | **Hook revisions** (task `hook-revisions`). An approval must cover what runs, and today it does not: `spec_hash` hashes the target only, and the hook that is dispatched is whatever sits in Nomad under its fixed ID at that moment (the latest in git), with a race between two deployments sharing a hook since Nomad has no CAS on dispatch. A deployment will freeze the specs of its hooks and hash them with the target; each hook revision is registered as `<hook-id>-<8 hex>` right before its dispatch, from the frozen spec, and unused revisions are deregistered without purge. This is the Nomad reading of "approve a revision, not an object" (Argo CD creates its sync hooks from the synced revision at sync time): a `parameterized` job is still registered once and dispatched many times, and a version that two deployments share is one job. Not chosen: a hook job per deployment (Argo's model; many more jobs, and cleanup on recovery) and a lock per hook ID inside nops (it cannot see someone registering the hook by hand). Registering right before dispatch, rather than at detection, keeps the cluster untouched until the OK and removes the per-cycle hook sync; its cost is one register the first time a revision is used. GC keeps only the revisions in use: the history is in nops (the frozen spec) and in Nomad's own dispatched jobs. No migration for hooks registered under a fixed ID: nops is not in use yet. |
| 2026-09-25 | **Multiple hooks** (task `multi-hooks`). `nops_pre_hook` / `nops_post_hook` take a comma-separated list, run in order and stop at the first failure; no parallel runs (not needed for a personal cluster). The timeout moves from the target (`nops_pre_hook_timeout`, `nops_post_hook_timeout`, removed) to the hook job's own `nops_timeout`: the hook's author knows how long it takes, a hook shared by several jobs keeps one limit, and with hook revisions it is frozen and approved with the rest. Not chosen: a plural key (`nops_pre_hooks`, a second name to explain), one timeout per phase on the target (a 20-minute backup and a 1-minute migration would share it), and inline timeouts (`db-backup:20m`, a string format to parse and read). Several tasks in one hook job, ordered with Nomad's `lifecycle`, stays possible but loses separate timeouts, a clear "which step failed" and reuse across jobs. |
| 2026-09-25 | **Orphan jobs** (task `orphan-jobs`): a job nops deployed that is no longer in the repository but still runs in Nomad is **shown, never stopped**, as Argo CD does by default ("OutOfSync, requires pruning"). Stopping a service is not something a deleted file should do on its own: a file that fails to parse, a wrong `-git-path` or a push to the wrong branch look the same as a removal, and here the worst case of a mistake is a wrong alert. For the same reason the check is suspended in a cycle with an unparsed file, and a job still in the repository without `nops_managed` is not an orphan (that means "hands off"). nops still never deregisters a job it deploys. Not chosen for now: a removal deployment under approval, and pruning under `auto` behind an opt-in; either can be built later on this observation, and a guard against mass removals belongs to that step. |
| 2026-09-25 | **Orphan jobs built** (`orphan-jobs`), refining the definition of the 2026-09-25 row: besides "not stopped", an orphan must not be `dead` in Nomad. A finished batch job has `Stop = false` and status `dead`, and "still running in Nomad" would be false for it: with no schedule nothing will run again, so it is not something to decide about. A periodic or parameterized parent stays `running`, so it is still reported. Candidates are the jobs with a **`completed` deployment** (`Store.LatestCompletedPerJob`, a new query, no schema change): a `failed` or `superseded` deployment means nops never put the job in production, and an ID that merely matches someone else's job in the same namespace is not nops's business. The "still in git" test uses **every parsed job ID**, before classification, so unmanaged jobs, invalid meta, `none` policy and duplicate IDs are all "still there". While a file does not parse the check is **suspended and the last complete result kept**, not cleared: an empty list would read as "all fine" exactly when something is wrong, at the cost of a stopped orphan staying listed until the file is fixed (the Overview says so). One `GET` per candidate per cycle, uncached: the list is short. |
| 2026-09-25 | Orphans show as the sync state **Not in git** on Jobs (rows added for them: no file, the policy of the last completed deployment), as the **last** line of Needs attention (nothing is broken: a service nobody asked for keeps running, so it comes after approvals, blocked jobs, failures and meta errors) and as a notice on the job's page with the exact command (`nomad job stop -namespace <ns> <id>`, or put the file back). No button: the page offers nothing that changes anything, and a later "Stop" action is a separate task that would build on `Engine.Orphans()`. |
| 2026-09-25 | **Hook revisions built** (`hook-revisions`), the design of the 2026-09-25 row above, with what checking it turned up. Verified on `nomad agent -dev` 2.0.3: deregistering a `parameterized` parent without purge leaves its dispatched runs and their logs readable, and a run that is still going carries on; a stopped parent refuses a dispatch, shows as a plan difference (`Stop`) and is registered again only at its own index (index `0` is a CAS conflict, the job exists); a job registers under an ID different from its `Name`; an ID of several hundred characters is accepted, so `<id>-<8 hex>` hits no limit. And: the job list carries no `Meta` unless asked (`?meta=true`), and a dispatched run inherits its parent's, so GC filters on the parent too, or every run would look like a revision. `spec_hash` is the SHA-256 of the target's hash and one `phase:hook-id:hash` line per declared hook (`missing` for one not in the snapshot, so adding it changes the hash), and the target's own hash when it declares none: a job without hooks is unchanged. Frozen hooks live in `deployment_hooks` (migration `0003`), written with the deployment in one transaction; `hook_runs.hook_job_id` holds the revision. Apply registers the revision at the hook step: read the job, plan, `RegisterCAS` at the index read, none needed when the plan is empty. A registration that fails for the hook's timeout (counted from `updated_at`, when the deployment entered the phase) fails the deployment, so a hook Nomad rejects cannot leave it in `pre_hook` forever, as the old flow (no register, hook not found) could not either. A deployment already in a hook phase with no frozen hook (made before this) fails with a message saying so instead of looping. The hook timeout still comes from the target's `nops_*_hook_timeout`; `multi-hooks` moves it. GC: after every cycle, stop the jobs that are `nops_role = "hook"`, match `^.+-[0-9a-f]{8}$`, have no parent and are not stopped, minus the revisions of non-terminal deployments; the store is read after the listing. Known and accepted: a hook registered by hand under an ID shaped like a revision is stopped. Replaces the 2026-09-24 row on the per-cycle hook sync. |
| 2026-09-25 | The consequences of hashing the hooks, chosen and tested: a hook changed in git supersedes a deployment waiting for approval (it must be approved again), a job blocked by a failed deployment is unblocked by fixing its hook, and a hook changed while the target has no drift creates no deployment. The alternative for the last one (a deployment for a hook change alone) was rejected: a deployment without a change to the target would register nothing and only run hooks around no apply. |
| 2026-09-25 | **Multiple hooks built** (`multi-hooks`), as decided in the row above. `Config.PreHooks` / `PostHooks` are lists of hook job IDs (`[]string`: with the timeout gone, a `Hook` struct would hold one field); a list is split on commas with spaces trimmed, and an empty item, a stray comma or the same hook twice in a phase is an ERROR on the key (policy `none`); the same hook in both phases is allowed. `nops_timeout` is read into `Config.Timeout` for a job with `nops_role = "hook"` (default 5m; invalid or non-positive is an ERROR and keeps the default; on a job that is not a hook it is a WARN, ignored). The two old timeout keys are reported as unknown keys with a message that says what replaces them, rather than silently: someone who had `nops_pre_hook_timeout = "30m"` would otherwise be running with 5m. `hook_runs` gets `position` (migration `0004`, a table rebuild because SQLite cannot change a `UNIQUE`); the token is `<deployment_id>:<phase>:<position>`, and rows that exist keep the token they were dispatched with, since a run in flight is found again by it. `EnsureHookRun` and `GetHookRun` take the position, `ListHookRuns` replaces asking phase by phase. `hooks.Runner` gains `Request.Position` and nothing else: one call is still one hook. `stepHook` walks the frozen hooks of the phase in position order inside one step: register the revision, run it, stop at the first failure; the step starts over after an error and the runner answers finished runs from the store, so it resumes at the first unfinished hook without a new column. A registration that keeps failing is bounded by the hook's own `nops_timeout` counted from when the previous hook finished (from the run's `finished_at`), so a long backup does not use up the migration's time. The failure message names the hook (`pre-hook backup failed: ...`). A hook whose meta has an error counts as unusable at detection, like a missing one, and is part of the hash (`invalid`) so fixing it unblocks the job. The `spec_hash` line per hook keeps the format of `hook-revisions` (its order is the position), so a job with one hook per phase keeps its hash. Observation carries `HookRef{JobID, Timeout}` per hook, the timeout from the hook file in the repo, none if it is missing or invalid. Not chosen: running the hooks of a phase in parallel, or a step per hook in the state machine (the states do not change: `pre_hook` is "the pre-hooks are running"). |
| 2026-09-25 | A deployment that ends asks for a detection cycle. The dashboard's sync state combines the job's latest deployment (read from the store, always current) with the last cycle's observation (Drift, BlockedBy), which is only rebuilt on a new commit, on the `-drift-interval` tick (5m) or on a retry. So after `completed` the Jobs and Job pages read **Drift** for up to five minutes although the job was in sync, and after `failed` or `rejected` they read Drift instead of Blocked. `applyTransition` (apply's own moves) kicks the loop through the channel `Retry` already used, now behind `kickDetection`, when the new state is `completed` or `failed`, and so does `Reject`; the moves detection makes itself do not (a cycle that kicked itself would queue another behind it). Reproduced first: an end-to-end test with `NOPS_DRIFT_INTERVAL=1h` never showed "In sync" after an approved deployment completed. Not chosen: having the page treat a deployment that ended after the observation as in sync, which hides stale data instead of refreshing it, leaves the stale diff on the Job page and does nothing for Blocked. |
| 2026-09-25 | Overview, Jobs, Job and Activity poll their own address every 5s and pick out only the region a full render already produces (`hx-select="#live"`, `hx-swap="outerHTML"`), the same htmx primitives the Deployment page's status fragment already uses, rather than a dedicated fragment endpoint per page or SSE/websockets: every page here is a cheap read (local SQLite, or the engine's and git watcher's in-memory state), so a second render path would only be more code for the same cost. `baseData.Self` (`r.URL.RequestURI()`) is what each page polls, so a page's own `?state=` filter survives the poll without extra plumbing. The Job page splits into three such regions (`#live-head`, `#live-deployments`, `#live-details`) so the drift diff between them, whose `<details>` nodes a person may have opened or closed by hand, is never sent again by the poll. |
| 2026-09-25 | The Overview's git strip read "Iacopo Melani · 3m ago … checked just now" as if it were one moment: the first is when the commit was made, the second when nops last polled the repository, and with the default one-minute poll interval the second reads "just now" almost always. Relabelled "committed 3m ago" and "repository checked just now" so the two clocks cannot be misread as one. |
